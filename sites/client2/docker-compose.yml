services:
  client2:
    image: nginx:alpine
    container_name: client2
    restart: unless-stopped
    volumes:
      - ./html:/usr/share/nginx/html:ro
    networks:
      - caddy-net

networks:
  caddy-net:
    external: true
